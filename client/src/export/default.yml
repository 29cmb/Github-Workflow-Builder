# Generated by Overflow

# Components that aren't connected to actions 
# will NOT be displayed!

name: Workflow

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  